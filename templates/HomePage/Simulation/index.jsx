"use client";

const Simulation = () => {
  return (
    <section
      className="relative w-full"
      style={{
        minHeight: "200vh",
        background: "#0D0D0D",
        maskImage:
          "linear-gradient(to bottom, transparent, #0D0D0D 20% 80%, transparent)",
        WebkitMaskImage:
          "linear-gradient(to bottom, transparent, #0D0D0D 20% 80%, transparent)",
      }}
    >
      {/* Simulation content goes here */}
    </section>
  );
};

export default Simulation;
